import { connect } from "react-redux"; 

import Splash from "./splash" ; 

const msp = (state) => {
    return {};
}

const mdp = dispatch => ({
    
})

export default connect(
    msp,mdp
)(Splash); 